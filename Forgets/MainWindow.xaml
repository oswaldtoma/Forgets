<Window x:Class="Forgets.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Forgets"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <TabControl HorizontalAlignment="Left" Width="790" Margin="0,0,0,1">
        <TabItem Header="Calendar" Margin="-2,-2,-164,0" Height="34" VerticalAlignment="Top">
            <Grid Background="#FFE5E5E5" Margin="0,0,0,-6">
                <Grid Margin="0,0,0,-3">
                    <!--<Grid.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="#FF004080" Offset="0"/>
                            <GradientStop Color="#FFABD5FF" Offset="1"/>
                        </LinearGradientBrush>
                    </Grid.Background>-->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Calendar x:Name="Calendar" Grid.Column="0" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" SelectedDatesChanged="Calendar_SelectedDatesChanged" Height="166" Width="183"/>
                    <ListView x:Name="dailySchedule" ItemsSource="{Binding appointments}" Grid.ColumnSpan="2" Margin="198,0,0,0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding RecordType}"/>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding RecordName}"/>
                                <GridViewColumn Header="Start Time" DisplayMemberBinding="{Binding StartTime}"/>
                                <GridViewColumn Header="End Time" DisplayMemberBinding="{Binding EndTime}"/>
                                <GridViewColumn Header="Location" DisplayMemberBinding="{Binding Location}"/>
                                <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button x:Name="test" Content="Button" HorizontalAlignment="Left" Margin="87,306,0,0" VerticalAlignment="Top" Width="75" Click="test_Click"/>
                </Grid>
            </Grid>
        </TabItem>
        <TabItem Header="TabItem" Margin="164,-2,-326,2" RenderTransformOrigin="1.147,0.539">
            <Grid Background="#FFE5E5E5"/>
        </TabItem>
    </TabControl>
</Window>
